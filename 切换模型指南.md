# Fish Speech 模型切换指南

## 📋 概述

您现在有两个TTS模型可用，它们都使用**8445端口**（互斥运行）：

| 模型 | 启动脚本 | 特点 | 建议场景 |
|------|---------|------|---------|
| **S1-Mini** | `./start_s1_mini.sh` | 最新、更强、10 codebooks | 追求最佳音质 |
| **Fish 1.5** | `./start_fish15_api.sh` | 稳定、轻量、8 codebooks | 追求速度和稳定 |

---

## 🔄 快速切换

### 从 Fish 1.5 切换到 S1-Mini

```bash
# 1. 停止 Fish 1.5
./start_fish15_api.sh --stop

# 2. 启动 S1-Mini
./start_s1_mini.sh --daemon

# 3. 验证
curl -X POST http://127.0.0.1:8445/v1/health
```

### 从 S1-Mini 切换到 Fish 1.5

```bash
# 1. 停止 S1-Mini
./start_s1_mini.sh --stop

# 2. 启动 Fish 1.5
./start_fish15_api.sh --daemon

# 3. 验证
curl -X POST http://127.0.0.1:8445/v1/health
```

---

## 🚀 一键切换脚本

### 创建切换脚本

```bash
# 创建 switch_to_s1mini.sh
cat > switch_to_s1mini.sh << 'EOF'
#!/bin/bash
echo "切换到 S1-Mini..."
./start_fish15_api.sh --stop 2>/dev/null
sleep 2
./start_s1_mini.sh --daemon
EOF

# 创建 switch_to_fish15.sh
cat > switch_to_fish15.sh << 'EOF'
#!/bin/bash
echo "切换到 Fish 1.5..."
./start_s1_mini.sh --stop 2>/dev/null
sleep 2
./start_fish15_api.sh --daemon
EOF

# 添加执行权限
chmod +x switch_to_s1mini.sh switch_to_fish15.sh
```

### 使用切换脚本

```bash
# 切换到 S1-Mini
./switch_to_s1mini.sh

# 切换到 Fish 1.5
./switch_to_fish15.sh
```

---

## 🌐 从 Windows 访问

由于两个模型都使用**8445端口**，您只需配置一次端口转发：

### 配置端口转发（仅需一次）

在 Windows PowerShell（**管理员权限**）运行：

```powershell
cd \path\to\fishtts
.\setup_wsl2_port_forward.ps1
```

### 访问方式

配置后，无论运行哪个模型，都使用相同地址：

```powershell
# 从 Windows 访问
curl http://localhost:8445/v1/health

# 生成语音
curl -X POST http://localhost:8445/v1/tts `
  -H "Content-Type: application/json" `
  -d '{"text":"你好世界"}' `
  -o output.wav
```

---

## 📊 模型对比

### S1-Mini (推荐)

**优势**:
- ✅ 音质更好（WER: 0.011 vs 0.015）
- ✅ 支持更多语言（13+ vs 8+）
- ✅ RLHF训练，更自然
- ✅ 10 codebooks，表现力更强

**劣势**:
- ⚠️ 内存占用稍高（~5GB vs ~4GB）
- ⚠️ 生成速度稍慢

**适合**:
- 要求最佳音质
- 多语言混合
- 声音克隆

### Fish 1.5

**优势**:
- ✅ 速度更快
- ✅ 内存占用低
- ✅ 久经验证，稳定

**劣势**:
- ⚠️ 音质略逊于S1-Mini
- ⚠️ 语言支持较少

**适合**:
- 要求快速响应
- 资源受限环境
- 大批量生成

---

## 🧪 A/B 测试建议

### 测试相同文本

```bash
# 使用 S1-Mini 生成
./start_s1_mini.sh --daemon
curl -X POST http://127.0.0.1:8445/v1/tts \
  -H "Content-Type: application/json" \
  -d '{"text":"这是一段测试文本"}' \
  -o test_s1mini.wav

# 切换到 Fish 1.5
./start_s1_mini.sh --stop
./start_fish15_api.sh --daemon
curl -X POST http://127.0.0.1:8445/v1/tts \
  -H "Content-Type: application/json" \
  -d '{"text":"这是一段测试文本"}' \
  -o test_fish15.wav

# 对比文件
ls -lh test_*.wav
# 播放对比音质
```

### 测试场景建议

1. **短句测试** (5-10字)
   - 评估响应速度
   - 对比清晰度

2. **长句测试** (50-100字)
   - 评估稳定性
   - 对比自然度

3. **多语言测试**
   - 中英混合
   - 其他语言支持

4. **声音克隆测试**
   - 使用参考音频
   - 对比相似度

---

## ⚙️ 高级配置

### S1-Mini 性能优化

```bash
# 使用半精度（减少内存50%）
API_HALF=true ./start_s1_mini.sh --daemon

# 启用编译加速（提速30-50%）
API_COMPILE=true ./start_s1_mini.sh --daemon

# 组合使用
API_HALF=true API_COMPILE=true ./start_s1_mini.sh --daemon
```

### Fish 1.5 性能优化

```bash
# 同样支持半精度和编译
API_HALF=true API_COMPILE=true ./start_fish15_api.sh --daemon
```

---

## 📝 查看当前运行的模型

```bash
# 检查 S1-Mini
./start_s1_mini.sh --status

# 检查 Fish 1.5
./start_fish15_api.sh --status

# 检查端口占用
lsof -i :8445

# 查看进程
ps aux | grep "api_server.py"
```

---

## 🔍 日志查看

```bash
# S1-Mini 日志
tail -f s1_mini.log

# Fish 1.5 日志
tail -f fish15_api.log
```

---

## 🆘 常见问题

### 1. 端口被占用

**症状**: 启动失败，提示 "Address already in use"

**解决**:
```bash
# 检查哪个服务在运行
lsof -i :8445

# 停止所有服务
./start_s1_mini.sh --stop
./start_fish15_api.sh --stop

# 重新启动需要的服务
./start_s1_mini.sh --daemon
```

### 2. 忘记当前运行的是哪个模型

**解决**:
```bash
# 检查日志文件修改时间
ls -lt s1_mini.log fish15_api.log | head -3

# 或查看进程命令行
ps aux | grep "api_server.py" | grep -v grep
```

### 3. 从 Windows 访问失败

**检查**:
```bash
# 在 WSL2 中检查服务
curl http://127.0.0.1:8445/v1/health

# 检查当前 WSL2 IP
hostname -I
```

**解决**:
```powershell
# 在 Windows PowerShell 管理员模式重新配置
.\setup_wsl2_port_forward.ps1
```

---

## 💡 使用建议

### 日常使用

**推荐**: 默认使用 **S1-Mini**
- 音质更好
- 功能更强
- 是Fish Speech的未来方向

### 特殊场景

**使用 Fish 1.5** 当：
- 需要快速批量生成
- 资源（内存/GPU）受限
- 需要最大稳定性

---

## 📚 相关文档

- **S1-Mini 升级报告**: `S1_MINI_UPGRADE_COMPLETE.md`
- **Fish 1.5 快速开始**: `FISH15_QUICK_START.md`
- **使用说明**: `使用说明.md`
- **问题排查**: `S1_MINI_SOLUTION_GUIDE.md`

---

## 🎯 快速参考

### 常用命令

```bash
# 启动 S1-Mini（推荐）
./start_s1_mini.sh --daemon

# 启动 Fish 1.5
./start_fish15_api.sh --daemon

# 停止服务
./start_s1_mini.sh --stop
./start_fish15_api.sh --stop

# 查看状态
./start_s1_mini.sh --status
./start_fish15_api.sh --status

# 测试 API
curl -X POST http://127.0.0.1:8445/v1/health

# 生成语音
curl -X POST http://127.0.0.1:8445/v1/tts \
  -H "Content-Type: application/json" \
  -d '{"text":"测试"}' \
  -o output.wav
```

### 端口说明

- **8445**: TTS服务端口（S1-Mini 或 Fish 1.5）
- **两个模型不能同时运行**（共用端口）
- **从Windows访问使用相同配置**

---

**更新时间**: 2025-11-05
**建议**: 优先使用S1-Mini获得最佳体验
